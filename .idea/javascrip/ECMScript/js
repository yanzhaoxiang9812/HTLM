
   JavaScript包括三大块：
        ECNAScript: 3s的核心语法（Es规范│ECMA-262标准)
        DOM: Document Object Model(文档对象模型。对网页当中的节点进行增删改的过程。)HTML文档被当做一棵ooN树来看待。var dom0bj = document.getElementById("id");
        BOMN: Browser 0bject Model (浏览器对象模型)
        关闭浏览器窗口、打开一个新的浏览器窗口、后退、前进、浏览器地址栏上的地址等，都是Bow编程。
